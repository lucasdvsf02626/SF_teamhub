{
  "name": "@sf/core",
  "version": "0.0.0",
  "private": true,
  "description": "Business logic shared by the web app and the React Native app. Pure TypeScript — no DOM, no React Native, no Supabase client. Anything here must run unchanged on both.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "date-fns": "^3.6.0"
  }
}
